// ============================================================
// HomePage.tsx
// This is the simplest page in the app.
// It serves as the landing page when users first visit the site.
// You can expand this later with featured products, banners,
// or navigation shortcuts.
// ============================================================

const HomePage = () => {
  // The component returns a simple heading for now.
  // React components must return a single JSX element.
  return <h1>Welcome to the Shopping App</h1>;
};

export default HomePage;